.claude/settings.local.json
__pycache__/
*.pyc

# Python build artifacts from `pip install .` / `python -m build` of the
# standalone plan-keeper CLI (plugins/plan-keeper/scripts/pyproject.toml).
build/
dist/
*.egg-info/

# Generated/working exec-plans and task-list state. These are scratch
# artifacts for in-flight work, never committed (the shipped plans live in
# the user's local ~/plans/<repo>/ tree, not in the repo).
docs/exec-plans/
